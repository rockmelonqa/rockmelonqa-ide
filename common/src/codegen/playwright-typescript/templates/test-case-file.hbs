import { Page, expect } from "@playwright/test";
import { TestCaseBase } from "~/support/TestCaseBase";
import EnvironmentSettings from "~/config/EnvironmentSettings";

{{#if routineImports.length}}
{{#each routineImports as |routineImport|}}
{{{routineImport}}}
{{/each}}
{{/if}}

{{#if description}}
/** {{{description}}} */
{{/if}}
export default class extends TestCaseBase {
  constructor(page: Page) {
    super(page);
    this.pageTest = this;
  }

  public async run() {
{{{body}}}
  }
}
