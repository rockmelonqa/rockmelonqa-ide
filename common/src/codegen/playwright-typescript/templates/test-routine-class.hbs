{{#if description}}
/** {{{description}}} */
{{/if}}
export class {{{testRoutineName}}} extends TestRoutineBase {
  constructor(page: Page) {
    super(page);
  }

  public async run() {
    let defs: PageDefinitions = new PageDefinitions(this.page);

{{{body}}}
  }
}