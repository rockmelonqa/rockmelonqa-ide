import { Page, Locator, FrameLocator } from "playwright";

/** {{{pageDescription}}} */
export class {{{pageName}}} {
    private _page: Page;

    constructor(page: Page) {
        this._page = page;
    }

{{{pageBody}}}
}