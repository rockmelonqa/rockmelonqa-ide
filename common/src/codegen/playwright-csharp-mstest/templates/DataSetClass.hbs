/// <summary>
/// DataSets for test routine {{{testRoutineName}}}
/// </summary>
public class {{testRoutineName}}DataSets
{
    {{#each datasets as |dataset dsIndex|}}
    {{#if dataset.description}}
    /// <summary>
    /// {{{dataset.description}}}
    /// </summary>
    {{/if}}
    public static DataSetBase DataSet{{dsIndex}} = new () 
    {
        Values = new[]
        {
            {{#each dataset.values as |value|}}
            "{{{value}}}",
            {{/each}}
        }
    };
    
    {{/each}}
}