namespace {{{fullNamespace}}};

{{#if pageDescription}}
/// <summary>
/// {{{pageDescription}}}
/// </summary>
{{/if}}
public class {{{pageClassName}}}
{
    private IPage _page;

    /// <summary>
    /// Default constructor
    /// </summary>
    /// <param name="page">Playwright Page</param>
    public {{{pageClassName}}}(IPage page)
    {
        this._page = page;
    }

{{{pageBody}}}
}