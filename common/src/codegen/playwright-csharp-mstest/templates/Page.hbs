namespace {{{fullNamespace}}};

{{#if pageDescription}}
/// <summary>
/// {{{pageDescription}}}
/// </summary>
{{/if}}
public class {{{pageClassName}}}
{
    private IPage _page;

    /// <summary>
    /// Default constructor
    /// </summary>
    /// <param name="page">Playwright Page</param>
    public {{{pageClassName}}}(IPage page) => _page = page;

{{{pageBody}}}
}